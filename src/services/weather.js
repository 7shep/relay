import { weatherDescription } from '../utils/formatting.js'
export async function loadWeather(latitude, longitude, signal) { const params = new URLSearchParams({ latitude: String(latitude), longitude: String(longitude), current: 'temperature_2m,relative_humidity_2m,apparent_temperature,weather_code,wind_speed_10m', hourly: 'temperature_2m,precipitation_probability', daily: 'temperature_2m_max,temperature_2m_min', temperature_unit: 'celsius', wind_speed_unit: 'kmh', timezone: 'auto', forecast_days: '1' }); const response = await fetch(`https://api.open-meteo.com/v1/forecast?${params}`, { signal }); if (!response.ok) throw new Error('Weather request failed'); const data = await response.json(); const found = data.hourly.time.findIndex((time) => time >= data.current.time); const start = found < 0 ? 0 : found; const hourly = data.hourly.time.slice(start,start+6).map((time,index) => ({ hour: time.slice(11,13), temp: Math.round(data.hourly.temperature_2m[start+index]) })); let location = 'Current location'; try { const reverseParams = new URLSearchParams({ format:'jsonv2', lat:String(latitude), lon:String(longitude), zoom:'10', addressdetails:'1' }); const reverseResponse = await fetch(`https://nominatim.openstreetmap.org/reverse?${reverseParams}`, { signal }); if (reverseResponse.ok) { const address = (await reverseResponse.json()).address || {}; location = address.city || address.town || address.village || address.municipality || location } } catch { /* Forecast remains usable. */ } return { location, temp: Math.round(data.current.temperature_2m), feelsLike: Math.round(data.current.apparent_temperature), condition: weatherDescription(data.current.weather_code), high: Math.round(data.daily.temperature_2m_max[0]), low: Math.round(data.daily.temperature_2m_min[0]), windKmh: Math.round(data.current.wind_speed_10m), humidity: Math.round(data.current.relative_humidity_2m), hourly } }
